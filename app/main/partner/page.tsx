import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Partner | PET TAXI",
};

export default function PartnerPage() {
  return (
    <section className="routePage">
      <h1>Partner</h1>
      <p>Partner page is ready.</p>
    </section>
  );
}
