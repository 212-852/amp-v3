import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Driver | PET TAXI",
};

export default function DriverPage() {
  return (
    <section className="routePage">
      <h1>Driver</h1>
      <p>Driver page is ready.</p>
    </section>
  );
}
